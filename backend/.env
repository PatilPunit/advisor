

# Copy this file to ".env" (same folder, next to main.py) and fill in real values.
# .env is loaded automatically by python-dotenv on every uvicorn start.

# --- AI Mentor (Phase 9) ---
GROQ_API_KEY=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

# --- Database ---
DATABASE_URL=postgresql://postgres:future@localhost:5432/career_advisor

# --- Redis caching (Phase 12, Deliverable 8) ---
# Optional - if unset or Redis isn't running, the app runs fine without
# caching, just recomputes every time.
REDIS_URL=redis://localhost:6379/0

# --- JWT Authentication (Phase 12, Deliverable 6) ---
# CRITICAL: generate a real random secret before deploying anywhere real.
# Generate one with: python3 -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET_KEY=change-this-to-a-real-random-secret-before-deploying
